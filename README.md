# HX Design System

海信物流各系统 Design System 集合。

## 目录结构

- `xinxiaodi-design-system/` — 信小递 Design System

## 使用方式

每个子目录为一个独立的 Design System，包含设计令牌(tokens)、组件规范(components)、样式文件(css/js)和预览示例(preview/examples)。

详细说明请参阅各子目录内的文档。
